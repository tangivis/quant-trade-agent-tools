{
  "name": "@quant-trade/cli-bridge",
  "version": "0.3.1",
  "description": "Internal shared TS module: spawnQuantCli + 9 tool schema + parse helpers. Used by both pi and dsh adapters.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./spawn": "./src/spawn.ts",
    "./parse": "./src/parse.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typebox": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  }
}
