<!-- visibility: internal-only; sanitized -->
# Historical extraction cleanup note

This archived note records the original extraction of agent tooling from the product repository.
It is not an active merge request description and contains no repository coordinates or environment
identifiers.

The extraction established these durable boundaries:

- this repository owns the Intelligence Plane and its release artifacts;
- the product repository owns the Product/Data/Domain Plane;
- runtime integration uses published HTTP contracts rather than source imports;
- pi and dsh remain thin harness adapters; and
- neither MCP nor REST exposes broker or repository mutation tools.

Current architecture, security and delivery status are documented in `ARCHITECTURE.md`, `SECURITY.md`
and `docs/handoffs/CURRENT.md`.
